version: 1.0
runtime: php81
build:
  commands:
    pre-build:
      - php -m
    build:
      - echo no build command for this PHP project
      - php -m
run:
  command: php -S 0.0.0.0:3000 -t .
  network:
    port: 3000
    env: APP_PORT
